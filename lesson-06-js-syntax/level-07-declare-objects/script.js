// Level 07 — Declare 10 objects with properties
// TODO: Declare and initialize 10 object variables with >=3 properties each.

// Example:
// let userProfile = { name: "Ava", age: 28, isSubscriber: false };

const userProfile = {name:"Kim", age: 30, isStudent: true };

const school = {name:"Salmen", year: 2002, hasGraduate: true };

const car = {make:"Chevrolet", model:"Traverse", color:"black" };

const house = {type:"ranch", story: 2, hasYard:true };

const animal = {speices:"elephant", color:"grey", hasFourLegs: true };

const book = {genre:"urban", cover:"black with roses", hasHappyEnd: false };

const bike = {make:"Kawasaki",founded:1878, isJapan: true };

const toy = {type:"lego", color:"multi-color", isSmall: true };

const playground = {material:"wood", ground:"sand", hasSswings:false };

const store = {type:"retell", product:"food", hasclothes:false };

export default userProfile;
