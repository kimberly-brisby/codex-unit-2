// Level 09 — Practice: Navigate and run
// TODO: Export a string array of commands.
export default ["I was able to use ls to list all the folders, then I used cd to find the one folder that had the node that I needed."];